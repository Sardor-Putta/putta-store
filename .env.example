# Copy to .env.local for local development (vercel dev picks it up automatically).
# In production, set these in the Vercel project's Environment Variables settings —
# never commit real values.

TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
